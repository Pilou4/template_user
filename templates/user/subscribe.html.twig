{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
<main class="subscribe">
    <h1>Créer un compte</h1>
    {{ form_start(form) }} 
        <div class="form__group">{{ form_row(form.email) }}</div>
        <div class="form__group">{{ form_row(form.password.first) }}</div>
        <div class="form__group">{{ form_row(form.password.second) }}</div>
        <button class="submit__form" type="submit">
            S'inscrire
        </button>
    {{ form_end(form) }}
</main>
{% endblock %}
