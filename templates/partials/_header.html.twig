<header>
    <nav>
        <a href="{{ path('homepage') }}">accueil</a>
        {% if app.user == null %}
            <a href="{{ path('user_subscribe') }}">inscription</a>
        {% endif %}
        {% if app.user %}
            <a href="{{ path('app_logout') }}">se deconnecter</a>
            {{ include('user/_delete_form.html.twig') }}
        {% else %}
            <a href="{{ path('app_login') }}">se connecter</a>
        {% endif %}
        
    </nav>
</header>